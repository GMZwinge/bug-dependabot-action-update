# bug-dependabot-action-update

The repo that demonstrate the dependabot issue where dependabot uses v1 as latest if an action uses that version,
then doesn't create a PR to update other versions to v1.
